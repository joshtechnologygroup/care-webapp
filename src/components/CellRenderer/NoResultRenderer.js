import React from 'react';

// TODO: This can be more customisable, so we can pass messsage in config params
const NoResultRenderer = item => (
  <div className="ag-loading-comp">
    No Data Found
  </div>
);

export default NoResultRenderer;
