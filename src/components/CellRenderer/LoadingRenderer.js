import React from 'react';
import Loader from 'AppComponents/Loader';

// Here we can add our own custom code with loader
const LoadingRenderer = item => (
  <Loader />
);

export default LoadingRenderer;
